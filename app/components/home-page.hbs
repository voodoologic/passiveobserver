<video autoplay muted loop>
    <source src="background.mp4" type="video/mp4">
    browser does not support the video tag.
</video>

<LoginButton @submitAction={{this.showLogIn}}/>
<LogIn
    @shouldDisplay={{this.displayLogIn}}
    @submitAction={{this.hideLogIn}}
/>
{{yield}}
